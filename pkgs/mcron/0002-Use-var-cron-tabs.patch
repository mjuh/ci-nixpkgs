From 6cf7fec078f157d418c580095bd0db65ae9c0e55 Mon Sep 17 00:00:00 2001
From: Oleg Pykhalov <go.wigust@gmail.com>
Date: Tue, 3 Sep 2019 19:39:10 +0300
Subject: [PATCH 2/3] Use /var/cron/tabs

---
 src/mcron/scripts/mcron.scm | 6 ++++--
 1 file changed, 4 insertions(+), 2 deletions(-)

diff --git a/src/mcron/scripts/mcron.scm b/src/mcron/scripts/mcron.scm
index 6545ada..357ea1a 100644
--- a/src/mcron/scripts/mcron.scm
+++ b/src/mcron/scripts/mcron.scm
@@ -40,7 +40,8 @@ silently ignored."
             ((or guile-syntax? (regexp-exec guile-regexp file-name))
              (load file-name))
             ((regexp-exec vixie-regexp file-name)
-             (read-vixie-file file-name))))))
+             (read-vixie-file file-name))
+            (else (read-vixie-file file-name))))))
 
 (define (process-files-in-user-directory input-type)
   "Process files in $XDG_CONFIG_HOME/cron and/or ~/.cron directories (if
@@ -56,7 +57,8 @@ $XDG_CONFIG_HOME is not defined uses ~/.config/cron instead)."
                          (scandir dir)))
              (λ (key . args)
                (set! errors (1+ errors)))))
-         (list (string-append home-directory "/.cron")
+         (list "/var/cron/tabs"
+               (string-append home-directory "/.cron")
                (string-append (or (getenv "XDG_CONFIG_HOME")
                                   (string-append home-directory "/.config"))
                               "/cron")))
-- 
2.23.0

